#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "asm.h"

int	mystrcmp(const char *s1, const char *s2);

int	main(int ac, char **av)
{
    if (ac == 3)
    {
      printf("Official --> %i \n", strcmp(av[1], av[2]));
      printf("Asm --> %i \n", mystrcmp(av[1], av[2]));
    }
  return (0);
}
