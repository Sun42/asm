#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "asm.h"
/*int	mystrlen(char *str);*/

int	main(int ac, char **av)
{
  if (ac == 2)
    {
      printf("Official --> %i \n", strlen(av[1]));
      printf("Asm --> %i \n", mystrlen(av[1]));
    }
  return (0);
}
